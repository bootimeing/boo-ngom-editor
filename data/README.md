# 语言数据

本目录保存扩展运行时使用的三引擎语言目录，以及验证这些目录准确性的审计基线。

- 根目录 JSON：补全、悬停、语义高亮和审查直接读取的运行时数据。
- `audit-report/`：语言目录维护与完整回归使用的审计基线，不进入 VSIX。

临时导出、生成过程文件和修改前备份分别放入 `md-export/`、`temp/`、`backup-*`；这些目录均不进入源码仓库。
